<li>
	{{name}}

	<button class="change-status" data-id="{{id}}" data-status="{{sleep}}">
		{{#if sleep}}Eat Burger!{{else}}Request Refund!{{/if}}
	</button>
	<button class="remove-burger" data-id="{{id}}">
		Remove
	</button>
</li>
